# 이번 생성 대상: web

**Primary concept**: browser window

**방향**: rounded translucent glass browser frame + minimal warm-white content shapes.

실제 웹사이트 screenshot을 만들지 않는다. 텍스트를 생성하지 않는다. browser chrome(주소창, 탭, 버튼 등)은 극도로 단순화한다.
